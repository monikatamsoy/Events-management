var toggler = document.getElementById("togglePassword")

toggler.click = function() {
  console.log("hey")
  // var passfield = document.getElementById("setpassword")
  // passfield.attributes.type == "password" ? (passfield.attributes.type = "text") : (passfield.attributes.type = "password")
}
